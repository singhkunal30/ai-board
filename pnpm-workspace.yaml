packages:
  # apps/mobile is a standalone Expo (React Native) project with its own npm
  # install, intentionally excluded from the pnpm workspace so it builds with
  # standard Android tooling.
  - "apps/api"
  - "apps/web"
  - "apps/mcp"
  - "packages/*"
